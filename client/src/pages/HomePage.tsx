import { AppButton } from "@/shared/components";

export function HomePage() {
 return (
    <div>
        <AppButton variant="contained">
            Entrar com github
        </AppButton>
    </div>
    );
}